{
  "name": "factorycontrol-apk",
  "version": "1.0.0",
  "description": "FactoryControl - Sistema de Gestão Industrial (APK)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:apk": "node build-apk.js",
    "build:apk:studio": "node build-apk.js --studio",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap add android",
    "cap:open": "npx cap open android",
    "install:capacitor": "npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/camera @capacitor/local-notifications @capacitor/splash-screen --save"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/camera": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0"
  }
}